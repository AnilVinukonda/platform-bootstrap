apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-bootstrap-app.fullname" . }}
  labels:
    {{- include "platform-bootstrap-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platform-bootstrap-app.selectorLabels" . | nindent 4 }}
